# Optional: point the frontend at a non-default API base URL.
# Left unset, the Vite dev server proxy (see vite.config.ts) forwards
# /api/* to http://localhost:8000, and the Docker nginx image proxies
# /api/* to the `api` service — so this is rarely needed.
VITE_API_BASE_URL=/api/v1
